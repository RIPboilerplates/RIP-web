/*
 *
 * {{ properCase name }} constants
 *
 */

/* eslint-disable no-multi-spaces */
const PREFIX = '{{ constantCase name }}'

export const {{ constantCase actionName }}           = `${{ curly true }}PREFIX{{ curly }}/{{ constantCase actionName }}`
export const {{ constantCase actionName }}_PENDING   = `${{ curly true }}PREFIX{{ curly }}/{{ constantCase actionName }}_PENDING`
export const {{ constantCase actionName }}_FULFILLED = `${{ curly true }}PREFIX{{ curly }}/{{ constantCase actionName }}_FULFILLED`
export const {{ constantCase actionName }}_REJECTED  = `${{ curly true }}PREFIX{{ curly }}/{{ constantCase actionName }}_REJECTED`
/* eslint-enable no-multi-spaces */

export const INITIAL_STATE = {
  fetching: null,
  fetched:  null,
  error:    null,
}
