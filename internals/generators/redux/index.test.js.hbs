import {
  {{ camelCase actionName }},
} from '../'

import {
  {{ constantCase actionName }},
} from '../constants'

describe('{{ properCase name }} actions', () => {
  describe('{{ camelCase actionName }}', () => {
    it('has a type of {{ constantCase actionName }}', () => {
      expect({{ camelCase actionName }}().type).toEqual({{ constantCase actionName }})
    })
  })
})
