import {
  {{ camelCase actionName }}Action,
} from '../'

import {
  {{ constantCase actionName }},
} from '../constants'

jest.mock('../thunks', () => ({
  {{ camelCase actionName }}Thunk: jest.fn(() => 'YAY!!'),
}))

describe('{{ properCase name }} actions', () => {
  let dispatch
  let result

  beforeEach(() => {
    dispatch = jest.fn()
    result = null
  })

  describe('{{ camelCase actionName }}Action', () => {
    it('dispatches correctly', () => {
      {{ camelCase actionName }}Action()(dispatch)

      result = {
        type: {{ constantCase actionName }},
        payload: 'YAY!!',
      }
      expect(dispatch).toHaveBeenCalledWith(result)
    })
  })
})
