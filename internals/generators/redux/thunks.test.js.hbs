import {
  {{ camelCase actionName }}Thunk,
} from '../thunks'

describe('{{ properCase name }} thunks', () => {
  describe('{{ camelCase actionName }}Thunk', () => {
    it('resolves asynchronously', async () => {
      expect.assertions(1)
      await {{ camelCase actionName }}Thunk().resolves
        .toEqual('YAY!!')
    })
  })
})
