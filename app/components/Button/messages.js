import { defineMessages } from 'react-intl'

/*
 * Button Messages
 *
 * This contains all the text for the Button component.
 */
export default defineMessages({
  button: {
    id:             'app.components.Button.button',
    defaultMessage: 'Log Out Action!',
  },
})
